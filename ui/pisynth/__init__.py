"""pisynth — Raspberry Pi MIDI synth appliance UI (touch + framebuffer).

Package layout (ticket #308 layering):
  app.py   — controller: wiring, main loop, control server, dispatch.
  (io/ · core/ · ui/ · screens.py are extracted from app.py in later steps.)
"""
