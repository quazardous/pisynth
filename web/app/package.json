{
  "name": "pisynth-companion",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "description": "pisynth web companion — the phone side (#659). Built into ../static, served by the Pi.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test ../../tests/js/*.test.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.57.0",
    "vite": "^8.3.0"
  }
}
